import React, { useContext } from "react";

// type FoodProps = {
//   props: [];
// };

// export default function Input({ props }: FoodProps) {
//   const [items, updateItem] = useContext(Context);

//   return (
//     <input
//       type="text"
//       inputMode="numeric"
//       pattern="[0-9]*"
//       onChange={({ target }) => updateItem(type, index, target.value)}
//       name={name.replace(" ", "-").toLowerCase()}
//     />
//   );
// }
